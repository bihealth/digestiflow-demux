# Base dependencies for executing `digestiflow-demux`.

# Snakemake is used for scheduling and execution.
snakemake >=5.4.0

# We use toml for configuration
toml >=0.10.0

# We use attrs for no-boilerplate classes
attrs >=18.2.0

# We use requests for accessing the API
requests
